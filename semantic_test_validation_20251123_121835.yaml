# Auto-generated validation configuration
# Generated from profile of: semantic_test.csv
# Generated at: 2025-11-23 12:18:35
# File size: 0.0 MB, Rows: 5
# Recommended chunk size: 2,000,000 rows (Using maximum chunk size (2,000,000). Sufficient memory available.)

validation_job:
  name: "Validation for semantic_test.csv"
  description: "Auto-generated from data profile"

settings:
  chunk_size: 2000000  # Optimized for csv format, 7 validations (moderate complexity)
  max_sample_failures: 100

files:
  - name: "semantic_test"
    path: "/tmp/semantic_test.csv"
    format: "csv"

    validations:
      - type: "EmptyFileCheck"
        severity: "ERROR"
        # Prevent empty file loads

      - type: "RegexPatternCheck"
        severity: "ERROR"
        params:
          pattern: "^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}$"
          pattern_name: "Valid Email Format"
          field: "customer_email"
        # Email addresses detected - validate format (100.0% of samples match)

      - type: "RegexPatternCheck"
        severity: "WARNING"
        params:
          pattern: "^(?:\+?1[-.\s]?)?(?:\(?\d{3}\)?[-.\s]?)?\d{3}[-.\s]?\d{4}$"
          pattern_name: "Valid Phone Number"
          field: "phone_number"
        # Phone numbers detected - validate format (100.0% of samples match)

      - type: "MandatoryFieldCheck"
        severity: "ERROR"
        params:
          fields:
            - "transaction_amount"
        # 1 fields have >95% completeness

      - type: "RangeCheck"
        severity: "WARNING"
        params:
          field: "transaction_amount"
          min_value: -10.0
          max_value: 500.75
        # Values range from -10.0 to 500.75

      - type: "RangeCheck"
        severity: "ERROR"
        params:
          field: "transaction_amount"
          min_value: 0
          max_value: 500.75
        # Amount fields should be non-negative

      - type: "RowCountRangeCheck"
        severity: "WARNING"
        params:
          min_rows: 2
          max_rows: 10
        # Expect approximately 5 rows based on profile
