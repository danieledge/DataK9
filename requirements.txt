# Core dependencies
pandas>=2.0.0
pyarrow>=12.0.0
openpyxl>=3.1.0
pyyaml>=6.0
jinja2>=3.1.0
click>=8.1.0
jsonschema>=4.17.0

# Optional for very large datasets
dask[dataframe]>=2023.5.0

# Optional for bloom filter optimizations (10x faster duplicate detection)
# Install with: pip install pybloom-live
# pybloom-live>=4.0.0

# Progress bars
tqdm>=4.65.0

# Date parsing
python-dateutil>=2.8.0

# For colored terminal output
colorama>=0.4.6

# System resource monitoring (memory safety)
psutil>=5.9.0

# Phase 2 Profiler Enhancements (Temporal Analysis, PII Detection, Enhanced Correlation)
statsmodels>=0.14.0      # Temporal analysis (trend detection, seasonality)
scipy>=1.11.0            # Statistical functions for correlation
scikit-learn>=1.3.0      # Mutual information for enhanced correlation

# Optional async dependencies (for concurrent file validation)
# Install with: pip install aiofiles
# aiofiles>=23.0.0

# Optional database dependencies (for database validations)
# Install with: pip install sqlalchemy psycopg2-binary pymysql
# SQLAlchemy>=2.0.0
# psycopg2-binary>=2.9.0  # PostgreSQL
# PyMySQL>=1.0.0          # MySQL
# cx_Oracle>=8.3.0        # Oracle
# pyodbc>=4.0.0           # SQL Server
